# 2026-07-16 17:30 — Convert v2.0 standards handbook PDF to DOCX + Markdown

## What
User supplied `AI_Project_Standards_and_Checklist.pdf` (Version 2.0 · July 2026, the
consolidated edition) and asked for `.docx` and `.md` deliverables under the same base
name, at the external standards folder.

Produced at `D:\Users\Chaitanya\Desktop\Working_Projects_25-26\AppNova\AI_Project_Standards\`:
- `AI_Project_Standards_and_Checklist.md`
- `AI_Project_Standards_and_Checklist.docx`

## Approach
The source `.md` for this handbook was never committed (the 2026-07-14 "move out of repo"
commits logged only the changes files), so it could not be recovered from git history. The
Markdown was therefore transcribed from the supplied PDF and formatted to match the house
conventions already established by the neighbouring `..._v2.1.md` — same anchor scheme,
`| ☐ |` checklist tables, `> **AppNova mapping:**` blockquotes, glossary term style.

Content is v2.0 as supplied. The v2.1 verification apparatus (the ✅/◐/○ compliance tags,
"About the verification status" section, the lifecycle applicability note, and Appendix A)
is deliberately **not** carried over — that material belongs to the later edition.

## Build
The 2026-07-14 build helpers lived in a session scratchpad and were gone, so they were
recreated in this session's scratchpad (not committed):
- `pagebreaks.lua` — Word page break before each Part (`h1`) and each Topic/Chapter
  (`h2[id^=topic-/chapter-]`); title exempted so page 1 isn't blank; colspec widths zeroed
  so Word autofits columns.
- `style_reference.py` — styled `reference.docx` via python-docx (orange `#C0451E`
  headings, 1.15 line spacing, 6 pt paragraph spacing).

Command: `pandoc <md> -f gfm+smart -o <docx> --reference-doc=reference.docx
--lua-filter=pagebreaks.lua --metadata pagetitle=...` (no `--metadata title`, no `--toc` —
both would duplicate the title/Contents already in the body, per the 2026-07-14 fix).

## Verification
Markdown counts against the PDF: 6 Parts, 5 Chapters, 5 Topics, **30 rules** (5/6/7/6/6),
5 quick checklists, 16 Part-3 subsections (3.0–3.15), 35 glossary terms.

DOCX inspected via python-docx: title appears **once**; 16 page breaks; 87 headings;
24 tables, all `w:tblW w:type="auto" w:w="0"` (autofit). Every table's dimensions match
the source PDF — 7 principles, 12 lifecycle phases, 10 guardrails (5-col wide table intact),
9 PII areas, 27 PII checklist items, 30 hallucination items, 29 HITL items, 37 accuracy
items, 10 Go/No-Go gates, 20 roles.

## Scope
Documentation only, and the deliverables live outside the repo. No backend/frontend/config/
prompt code touched; no app behaviour changed, so no README sync required.

## Files
- Added (outside repo): `AI_Project_Standards_and_Checklist.{md,docx}` at the path above.
- Added (in repo): `changes/2026-07-16_1730.md` (this file)
